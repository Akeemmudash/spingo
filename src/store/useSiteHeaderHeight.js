mport { create } from "zustand";
import {
  BrownMale,
  CoolMale,
  ModelFemale,
  RedFemale,
  SmilingFemale,
} from "../assets";

const useReviewStore = create((set, get) => ({


  
}));
export default useReviewStore;
